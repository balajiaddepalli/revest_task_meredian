name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  backend-tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service:
          - backend/api-gateway
          - backend/user-service
          - backend/product-service
          - backend/order-service
          - backend/cart-service
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: ${{ matrix.service }}/package-lock.json
      - name: Install and test
        working-directory: ${{ matrix.service }}
        run: |
          npm ci
          npm test --if-present

  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install frontend deps
        run: |
          cd frontend && npm ci
          cd ../admin-frontend && npm ci
          cd ../e2e && npm ci
      - name: Install Playwright browsers
        run: cd e2e && npx playwright install chromium
      - name: Run Playwright (mocked suites)
        run: |
          cd e2e && npx playwright test --grep-invert "Docker Compose|live-api-smoke"

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Build customer frontend
        run: cd frontend && npm ci && npm run build
      - name: Build admin frontend
        run: cd admin-frontend && npm ci && npm run build
